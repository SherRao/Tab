services:
  db:
    image: postgres:17
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: tab
      POSTGRES_PASSWORD: tab
      POSTGRES_DB: tab
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
